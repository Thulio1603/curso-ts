export enum Caracteres {
  add = "+",
  subtract = "-",
  multiply = "*",
  divide = "/",
  point = ".",
  parenthesesRight = "(",
  parenthesesLeft = ")",
  backspace = "C",
}

export const operators = ["+", "-", "*", "/"];
